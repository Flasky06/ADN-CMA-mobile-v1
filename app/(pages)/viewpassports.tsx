import { StyleSheet, Text, View } from "react-native";
import React from "react";

const viewpassports = () => {
  return (
    <View>
      <Text>viewpassports</Text>
    </View>
  );
};

export default viewpassports;

const styles = StyleSheet.create({});
